# Django-BBS 博客系统 · 项目归纳文档

> 本文档梳理 Django-BBS 博客系统的项目结构、功能模块、数据模型、URL 接口路由、缓存策略和前端交互。

---

## 一、项目概况

| 项目 | 说明 |
| --- | --- |
| 项目名 | Django-BBS 博客系统（瓜子BBS） |
| Python | 3.10+（推荐 3.10/3.11，兼容 3.14） |
| Django | 5.2.17 |
| 数据库 | MySQL 5.7+（库名 `guazi_bbs`，用户 `root`，密码 `123123`） |
| 缓存 | Redis（django-redis 作为唯一缓存后端，Session 也落 Redis） |
| 前端 | Bootstrap 3.4.1 + jQuery 3.7.1 + AJAX + FormData |
| 扩展库 | mysqlclient、pillow、markdown、django-redis |

---

## 二、目录结构

```
ai_django_bbs/
├── manage.py                       # Django 命令入口
├── requirements.txt                # 依赖清单
├── guazi_bbs/                      # 项目配置包
│   ├── __init__.py
│   ├── settings.py                 # 主配置（MySQL/Redis/邮箱/media/static）
│   ├── urls.py                     # 主路由（include app01 + media 服务）
│   ├── wsgi.py                     # WSGI 入口
│   └── asgi.py                     # ASGI 入口
├── app01/                          # 业务应用
│   ├── __init__.py
│   ├── admin.py                    # 7 张模型注册到 Django Admin
│   ├── apps.py                     # 应用配置
│   ├── models.py                   # 7 张业务表 + BaseModel 抽象基类
│   ├── views.py                    # 20 个类视图（全部业务逻辑）
│   ├── urls.py                     # 子路由（全部 URL 映射）
│   ├── forms/
│   │   ├── __init__.py
│   │   └── user_forms.py           # 注册表单校验（局部/全局钩子）
│   ├── utils/
│   │   ├── __init__.py
│   │   ├── code.py                 # 图形验证码生成（Pillow）
│   │   ├── email_util.py           # 邮箱验证码发送（QQ SMTP）
│   │   ├── dors.py                 # 登录校验装饰器
│   │   └── md_util.py              # Markdown 图片处理工具
│   ├── templatetags/
│   │   ├── __init__.py
│   │   └── my_tag.py               # plain_text / md_safe 过滤器
│   └── migrations/
│       ├── __init__.py
│       ├── 0001_initial.py         # 初始建表
│       └── 0002_article_cover_comment_reply_to.py  # 封面 + @功能迁移
├── templates/                      # 全部 HTML 模板
│   ├── base.html                   # 全局母版（导航栏 + 修改密码模态框）
│   ├── index.html                  # 首页（三栏：分类/文章/搜索+热榜）
│   ├── site.html                   # 个人站点（侧边栏 + 文章列表）
│   ├── article_detail.html         # 文章详情（正文 + 点赞 + 评论@ + 目录）
│   ├── register.html               # 注册页
│   ├── login.html                  # 登录页（三重校验）
│   ├── set_avatar.html             # 修改头像页
│   ├── error404.html               # 404 页面
│   ├── sidebar.html                # 侧边栏片段（inclusion_tag 备用）
│   └── backend/                    # 后台管理模板
│       ├── base.html               # 后台母版（左侧 Tab 导航）
│       ├── backend.html            # 文章列表
│       ├── add_article.html        # 新增文章（封面 + MD文本 + .md上传）
│       ├── edit_article.html       # 编辑文章（封面回显 + 内容回显）
│       ├── category.html           # 分类管理
│       ├── tag.html                # 标签管理
│       ├── comment.html            # 评论管理
│       └── info.html               # 个人信息编辑
├── static/                         # 静态资源
│   ├── bootstrap-3.4.1-dist/       # Bootstrap 3 CSS/JS/fonts
│   ├── js/jquery-3.7.1.min.js      # jQuery
│   ├── css/                        # 自定义 CSS
│   └── image/                      # 静态图片
└── media/                          # 用户上传资源
    ├── avatar/                     # 用户头像 + default.png
    ├── article_cover/              # 文章封面图片
    └── css/                        # 用户自定义站点主题 CSS
```

---

## 三、数据模型（7 张表 + 1 抽象基类）

### 3.1 BaseModel（抽象基类，不建表）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| create_time | DateTimeField(auto_now_add) | 创建时间 |
| update_time | DateTimeField(auto_now) | 更新时间 |
| is_delete | BooleanField(default=False) | 逻辑删除标记 |

### 3.2 User（用户表，继承 AbstractUser + BaseModel）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| username | CharField | 用户名（继承 AbstractUser） |
| password | CharField | 密码（哈希，继承 AbstractUser） |
| email | EmailField | 邮箱（继承 AbstractUser） |
| age | IntegerField | 年龄，默认 0 |
| gender | SmallIntegerField | 性别：1男/2女/3保密 |
| phone | CharField(11) | 手机号 |
| avatar | ImageField | 头像，上传到 media/avatar/ |
| blog | OneToOneField→Blog | 一对一关联个人站点 |

### 3.3 Blog（个人站点表）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| site_name | CharField(64) | 站点名称 |
| site_title | CharField(64) | 站点标题 |
| site_theme | CharField(128) | 自定义主题 CSS 路径 |

### 3.4 Tag（标签表）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| name | CharField(32) | 标签名 |
| blog | ForeignKey→Blog | 所属站点（一对多） |

### 3.5 Category（分类表）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| name | CharField(32) | 分类名 |
| blog | ForeignKey→Blog | 所属站点（一对多） |

### 3.6 Article（文章表）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| title | CharField(128) | 文章标题 |
| content | TextField | 文章内容（Markdown 原文） |
| cover | ImageField | 文章封面（可选，上传到 media/article_cover/） |
| up_num | IntegerField | 点赞数（冗余计数） |
| down_num | IntegerField | 点踩数（冗余计数） |
| comment_num | IntegerField | 评论数（冗余计数） |
| blog | ForeignKey→Blog | 所属站点 |
| category | ForeignKey→Category | 所属分类（可空，SET_NULL） |
| tags | ManyToManyField→Tag | 标签（多对多） |

### 3.7 UpAndDown（点赞点踩表）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| user | ForeignKey→User | 操作用户 |
| article | ForeignKey→Article | 被操作文章 |
| is_up | BooleanField | True=赞 / False=踩 |
| 联合唯一 | (user, article) | 一人一文章仅一条记录 |

### 3.8 Comment（评论表）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| user | ForeignKey→User | 评论用户 |
| article | ForeignKey→Article | 被评论文章 |
| content | TextField | 评论内容 |
| parent | ForeignKey→self | 父评论（null=根评论） |
| reply_to | ForeignKey→User | 回复目标用户（@功能，null=根评论） |

---

## 四、功能模块

### 4.1 用户模块

| 功能 | 视图类 | 方法 | 说明 |
| --- | --- | --- | --- |
| 注册 | RegisterView | GET/POST | 用户名/密码/邮箱/站点标题校验 + 头像上传 + 自动创建 Blog |
| 登录 | LoginView | GET/POST | 三重校验：图形验证码 + 用户名密码 + 邮箱验证码 |
| 退出 | LogoutView | GET | 清 session，跳转首页 |
| 图形验证码 | GetCodeImage | GET | 生成 PNG 验证码图片，存 Redis（60s） |
| 邮箱验证码 | GetEmailCode | POST | 发送 6 位数字验证码到用户邮箱，存 Redis（300s） |
| 修改密码 | ChangePwdView | POST | 旧密码校验 + 新密码确认，AJAX |
| 修改头像 | SetAvatarView | GET/POST | 上传新头像替换旧头像 |

### 4.2 首页模块

| 功能 | 视图类 | 方法 | 说明 |
| --- | --- | --- | --- |
| 首页 | IndexView | GET | 全部文章列表（Redis 缓存 300s）+ 分类筛选 + 我赞过 + 我评论过 + 标题搜索 + 最新6篇 + 近10天热榜6篇 |

### 4.3 个人站点模块

| 功能 | 视图类 | 方法 | 说明 |
| --- | --- | --- | --- |
| 站点首页 | SiteView | GET | /index/\<username\>/ 展示作者文章列表 + 分类/标签/归档侧边栏 |
| 分类筛选 | SiteView | GET | /index/\<username\>/category/\<cid\>/ |
| 标签筛选 | SiteView | GET | /index/\<username\>/tag/\<tid\>/ |
| 日期归档 | SiteView | GET | /index/\<username\>/date/\<year\>-\<month\>/ |

### 4.4 文章详情模块

| 功能 | 视图类 | 方法 | 说明 |
| --- | --- | --- | --- |
| 文章详情 | ArticleDetailView | GET | Markdown 转 HTML 渲染 + 点赞点踩 + 评论树（含 @用户名）+ 目录锚点 + 作者最新6篇 |
| 点赞点踩 | UpDownView | POST | AJAX 无刷新更新赞/踩数字；禁止给自己文章赞踩；切换/取消/新建三种逻辑 |
| 发表评论 | CommentView | POST | AJAX 提交；支持根评论和子回复；保存 reply_to（@功能） |

### 4.5 后台管理模块

| 功能 | 视图类 | 方法 | 说明 |
| --- | --- | --- | --- |
| 文章列表 | BackendView | GET | 当前用户站点的全部文章（含已删除） |
| 新增文章 | AddArticleView | GET/POST | 文本框 Markdown + .md 文件上传 + 封面上传 + 分类/标签 |
| 编辑文章 | EditArticleView | GET/POST | 回显数据 + 封面替换 + 内容编辑 |
| 删除文章 | DelArticleView | POST | 逻辑删除 is_delete=True |
| 分类管理 | CategoryView | GET/POST | 增删改 AJAX（action: add/edit/del） |
| 标签管理 | TagView | GET/POST | 增删改 AJAX（action: add/edit/del） |
| 评论管理 | CommentManageView | GET | 查看自己发表的全部评论 |
| 删除评论 | CommentDelView | POST | 逻辑删除 + 评论数 -1 |
| 个人信息 | InfoView | GET/POST | 用户名/年龄/性别/手机号 + 站点 CSS 主题上传 |

---

## 五、URL 接口路由

### 5.1 用户认证接口

| 路径 | 方法 | 视图 | 说明 |
| --- | --- | --- | --- |
| `/register/` | GET/POST | RegisterView | 注册页面 / AJAX 注册 |
| `/login/` | GET/POST | LoginView | 登录页面 / AJAX 三重校验登录 |
| `/logout/` | GET | LogoutView | 退出登录（需登录） |
| `/get_code/?key=xxx` | GET | GetCodeImage | 图形验证码 PNG 图片 |
| `/get_email_code/` | POST | GetEmailCode | 发送邮箱验证码 |
| `/change_pwd/` | POST | ChangePwdView | 修改密码（需登录） |
| `/set_avatar/` | GET/POST | SetAvatarView | 修改头像（需登录） |

### 5.2 首页与内容接口

| 路径 | 方法 | 视图 | 说明 |
| --- | --- | --- | --- |
| `/` | GET | IndexView | 首页（支持 ?type=cat&id= / ?type=my_up / ?type=my_comment / ?type=search&q=） |
| `/index/<username>/` | GET | SiteView | 个人站点首页 |
| `/index/<username>/category/<cid>/` | GET | SiteView | 按分类筛选 |
| `/index/<username>/tag/<tid>/` | GET | SiteView | 按标签筛选 |
| `/index/<username>/date/<year>-<month>/` | GET | SiteView | 按日期归档筛选 |
| `/index/<username>/p/<article_id>/` | GET | ArticleDetailView | 文章详情 |
| `/up_down/` | POST | UpDownView | 点赞/点踩 AJAX（需登录） |
| `/comment/` | POST | CommentView | 发表评论 AJAX（需登录，含 reply_user） |

### 5.3 后台管理接口

| 路径 | 方法 | 视图 | 说明 |
| --- | --- | --- | --- |
| `/backend/` | GET | BackendView | 文章列表（需登录） |
| `/backend/add_article/` | GET/POST | AddArticleView | 新增文章（含封面上传） |
| `/backend/edit_article/<article_id>/` | GET/POST | EditArticleView | 编辑文章（含封面替换） |
| `/backend/del_article/` | POST | DelArticleView | 删除文章（逻辑删除） |
| `/backend/category/` | GET/POST | CategoryView | 分类管理（增删改 AJAX） |
| `/backend/tag/` | GET/POST | TagView | 标签管理（增删改 AJAX） |
| `/backend/comment/` | GET | CommentManageView | 评论管理列表 |
| `/backend/comment/del/` | POST | CommentDelView | 删除评论 |
| `/backend/info/` | GET/POST | InfoView | 个人信息编辑 + CSS 主题上传 |

---

## 六、缓存策略

| 缓存 Key | 用途 | 过期时间 | 清除时机 |
| --- | --- | --- | --- |
| `code:{随机串}` | 图形验证码 | 60s | 登录校验通过后立即删除 |
| `email_code:{username}` | 邮箱验证码 | 300s | 登录校验通过后立即删除 |
| `home_article_list` | 首页全部文章列表 | 300s | 发布/编辑/删除文章、点赞点踩、评论时清除 |
| `site_sidebar:{blog_id}` | 个人站点侧边栏数据 | 300s | 发布/编辑/删除文章、分类/标签增删改时清除 |

缓存后端：Redis（django-redis），Session 也存 Redis（14 天过期）。

---

## 七、前端交互（AJAX）

| 场景 | 接口 | 交互方式 |
| --- | --- | --- |
| 注册 | `/register/` | FormData（含头像文件）POST |
| 登录 | `/login/` | 表单序列化 POST |
| 获取邮箱验证码 | `/get_email_code/` | POST（60s 倒计时按钮） |
| 修改密码 | `/change_pwd/` | 模态框 AJAX POST |
| 修改头像 | `/set_avatar/` | FormData POST |
| 点赞点踩 | `/up_down/` | AJAX POST，无刷新更新数字 |
| 发表评论 | `/comment/` | AJAX POST，成功后刷新页面 |
| 回复子评论 | — | 点击"回复"按钮设置 parent_id + reply_user，显示 @提示 |
| 新增文章 | `/backend/add_article/` | FormData（含 .md 文件 + 封面图片）POST |
| 编辑文章 | `/backend/edit_article/<id>/` | FormData（含封面图片）POST |
| 分类/标签增删改 | `/backend/category/` `/backend/tag/` | AJAX POST（action 参数） |
| 删除文章/评论 | `/backend/del_article/` `/backend/comment/del/` | AJAX POST |

全局 CSRF：`base.html` 中 `$.ajaxSetup` 自动从 cookie 读取 `csrftoken` 并设置请求头。

---

## 八、安全机制

| 机制 | 说明 |
| --- | --- |
| 密码存储 | Django AbstractUser 自动 PBKDF2 哈希 |
| CSRF | 全局 CSRF 中间件 + AJAX 自动携带 X-CSRFToken |
| XSS | 模板默认转义；文章正文用 `md_safe` 过滤器轻量净化（剥离 script/iframe/onXxx/javascript:） |
| 登录校验 | `is_login_method` 装饰器，未登录返回 `{"code":400,"msg":"请先登录"}` |
| 文件后缀校验 | 头像/封面仅允许 jpg/jpeg/png/gif/bmp；CSS 主题仅允许 .css |
| 逻辑删除 | 全部删除操作设置 is_delete=True，不物理删除 |
| 联合唯一约束 | UpAndDown(user, article) 一人一文章仅一条态度记录 |

---

## 九、自定义模板标签/过滤器

| 名称 | 类型 | 用途 |
| --- | --- | --- |
| `plain_text` | filter | Markdown → 纯文本（用于列表摘要，剥掉 md 语法和 HTML 标签） |
| `md_safe` | filter | Markdown→HTML 后轻量 XSS 净化 + mark_safe（用于文章正文渲染） |

使用方式：模板顶部 `{% load my_tag %}`，然后 `{{ content|plain_text }}` 或 `{{ html|md_safe }}`。

---

## 十、工具函数

| 文件 | 函数 | 用途 |
| --- | --- | --- |
| `utils/code.py` | `get_code_image()` | 生成 4 位随机验证码 + PNG 图片二进制 |
| `utils/email_util.py` | `generate_email_code()` | 生成 6 位数字验证码 |
| `utils/email_util.py` | `send_email_code(to, code)` | 通过 QQ SMTP 发送验证码邮件 |
| `utils/dors.py` | `is_login_method` | 类视图方法登录校验装饰器 |
| `utils/dors.py` | `is_login_func` | 函数视图登录校验装饰器 |
| `utils/md_util.py` | `process_md_images(md, uid)` | 解析 .md 文件中图片引用，远程/base64 图片下载到本地 |
| `utils/md_util.py` | `read_md_file_content(f)` | 读取上传 .md 文件内容为字符串 |

---

文档结束。
